import React from "$react";
const AppFooterLayout = React.forwardRef((props,ref)=>{
    return <span>i am footer</span>
});

AppFooterLayout.displayName = "AppFooterLayout";

export default AppFooterLayout;